services:
  app:
    image: bkk-now
    container_name: bkk-now
    volumes:
      - ./data:/usr/share/nginx/html/data
    networks:
      - swag-network
    restart: unless-stopped

networks:
  swag-network:
    external: true
